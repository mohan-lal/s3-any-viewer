# Store listing copy

Everything needed to fill the Chrome Web Store, Microsoft Edge Add-ons and Firefox Add-ons forms. Character limits are the Chrome Web Store ones; Edge and Firefox are more generous, so the same text works everywhere.

---

## Identity

| Field | Value | Limit |
|---|---|---|
| **Name** | `S3 Any Viewer` | 45 |
| **Summary** (short description) | `View any S3 object in the browser, formatted: CSV, JSON, Parquet, Excel and many more. No AWS keys.` | 132 |
| **Category** | Developer Tools | |
| **Language** | English | |
| **Website / homepage** | `https://github.com/mohan-lal/s3-any-viewer` | |
| **Support URL** | `https://github.com/mohan-lal/s3-any-viewer/issues` | |
| **Privacy policy URL** | `https://github.com/mohan-lal/s3-any-viewer/blob/main/PRIVACY.md` | |

---

## Detailed description

Written as prose on purpose. The store's metadata policy treats descriptions that enumerate formats, brands or features as keyword lists and asks for fewer than five uses of any one term, so keep format names to a passing mention here; the full list lives in the README and the screenshots.

```
Stop downloading files just to look at them.

S3 Any Viewer turns the "Open" button in the AWS console into a real file viewer. Click Open on any object and it renders in a new tab, formatted for its type: CSV, JSON, Parquet, Excel, PDF and many more, including compressed and archived files. Tables you can sort and filter, trees you can fold, documents and media shown inline, and a hex dump for anything else instead of a silent download.

Every table supports sorting, substring filtering across all columns, column resizing, double-click to inspect a cell, and export to CSV, TSV, JSON or the clipboard.

NO AWS CREDENTIALS
The extension never asks for access keys, session tokens or cookies. The console already generates a short-lived presigned URL when you click it. The extension simply intercepts that navigation and renders the bytes in the browser. The only network request is the one that would have been made anyway. The console's Download button keeps working as before.

WORKS WITH
• Amazon S3 console (all regions, including AWS China)
• Any presigned link, via right-click → "Open link in S3 Any Viewer"
• Compatible object stores such as MinIO and Cloudflare R2, and CloudFront URLs, after a one-time permission prompt
• Local files, via the toolbar popup

PRIVACY
No analytics, no telemetry, no remote code. All parsing happens in your browser tab. The only stored data is two on/off preferences. Full policy: https://github.com/mohan-lal/s3-any-viewer/blob/main/PRIVACY.md

OPEN SOURCE
MIT licensed. Source, issues and roadmap: https://github.com/mohan-lal/s3-any-viewer
```

(≈1,700 characters; limit 16,000. No keyword appears more than four times across summary and description, excluding the product name and URLs.)

---

## Privacy tab (Chrome Web Store)

### Single purpose

```
Display Amazon S3 objects inside the browser in a readable, format-aware viewer instead of downloading them.
```

### Permission justifications

| Permission | Justification text |
|---|---|
| `declarativeNetRequest` | `Redirects top-level navigations to presigned S3 URLs (the URL the S3 console opens when the user clicks "Open") to the extension's viewer page, so the object is displayed instead of downloaded. Rules are limited to *.amazonaws.com main-frame requests that carry an AWS SigV4 signature. No request bodies or headers are read or modified.` |
| `storage` | `Stores two boolean user preferences: whether to intercept "Open" links and whether to also intercept "Download" links. Nothing else is stored.` |
| `contextMenus` | `Adds a single "Open link in S3 Any Viewer" entry to the link context menu so users can open a presigned link they received elsewhere.` |
| Host permission `*://*.amazonaws.com/*` and `*://*.amazonaws.com.cn/*` | `Required to fetch the bytes of the S3 object the user chose to open, using the presigned URL generated by the AWS console. The extension makes no other requests to these hosts.` |
| Optional host permission `<all_urls>` | `Requested only at runtime, and only when the user pastes a non-AWS URL into the popup (S3-compatible storage such as MinIO or Cloudflare R2, or a CloudFront distribution). The prompt names the exact origin; nothing is granted silently and the permission can be revoked at any time.` |

### Remote code

```
No, I am not using remote code.
```
All libraries are bundled at build time with esbuild. The WebAssembly module for Snappy decompression (Parquet) is embedded in the bundle; `'wasm-unsafe-eval'` in the CSP exists for that module only.

### Data usage

Tick **none** of the data collection boxes. The extension does not collect personally identifiable information, health, financial, authentication, personal communications, location, web history, user activity or website content.

Certifications, tick all three:

- I do not sell or transfer user data to third parties, outside of the approved use cases.
- I do not use or transfer user data for purposes that are unrelated to my item's single purpose.
- I do not use or transfer user data to determine creditworthiness or for lending purposes.

---

## Distribution tab

| Field | Value |
|---|---|
| Visibility | Public |
| Regions | All regions |
| Pricing | Free |

---

## Graphic assets

Generated by `npm run screenshots` into `store/assets/`:

| File | Size | Use |
|---|---|---|
| `screenshot-1-csv.png` | 1280×800 | Screenshot 1 (hero): CSV opened from S3 as a sortable table |
| `screenshot-2-parquet.png` | 1280×800 | Screenshot 2: Parquet with schema panel |
| `screenshot-3-json.png` | 1280×800 | Screenshot 3: JSON tree |
| `screenshot-4-xlsx.png` | 1280×800 | Screenshot 4: Excel workbook with sheet tabs |
| `screenshot-5-log.png` | 1280×800 | Screenshot 5: 60,000-line log with grep filter |
| `promo-small-440x280.png` | 440×280 | Small promo tile (required for featuring) |
| `promo-marquee-1400x560.png` | 1400×560 | Marquee promo tile (optional) |
| `icon-128.png` | 128×128 | Store icon (same as the manifest icon) |

Upload up to five screenshots on Chrome. Edge accepts up to ten and Firefox up to ten; reuse the same set.

---

## Firefox-specific fields

| Field | Value |
|---|---|
| Add-on ID | set in `browser_specific_settings.gecko.id` (planned Firefox build target) |
| License | MIT |
| Source code submission | Not required (no minification, but attach the GitHub URL in reviewer notes) |
| Reviewer notes | `Build from source with "npm ci && npm run build". The redirect rule only matches *.amazonaws.com main-frame URLs containing X-Amz-Signature=. Test by opening any presigned S3 URL.` |

---

## Review-risk checklist

- [x] Manifest V3
- [x] No remote code, no `eval`, CSP restricts scripts to `'self'`
- [x] Narrow host permissions; broad access is optional and user-initiated
- [x] Privacy policy hosted at a public URL and linked in the listing
- [x] Single purpose stated and matched by the description
- [x] Every permission justified
- [x] Screenshots show the actual product, no mock-ups
- [ ] Publisher email verified in the dashboard (do this once after registering)
